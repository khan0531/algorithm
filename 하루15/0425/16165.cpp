#include <bits/stdc++.h>

using namespace std;

int n, m;
map<string, string> name;
map<string, string> group;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> n >> m;

    string s;
    while(n--) {

    }
}
